- hosts: paz
  gather_facts: false
  tasks:
    - ansible.windows.win_package:
        path: '{{ temurin_jre_url }}'
        arguments: ADDLOCAL=FeatureMain,FeatureEnvironment,FeatureJavaHome,FeatureOracleJavaSoft /quiet
        state: present
