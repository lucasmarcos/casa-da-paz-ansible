- name: Check GCompris version
  hosts: paz
  gather_facts: false
  tasks:
    - name: Consultar versao do GCompris
      ansible.windows.win_shell: |
        (Get-ChildItem "HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Uninstall" |
          Get-ItemProperty |
          Where-Object { $_.DisplayName -like "*GCompris*" }).DisplayVersion
      register: gcompris_version

    - name: Exibir versao do GCompris
      ansible.builtin.debug:
        msg: "{{ inventory_hostname }}: {{ gcompris_version.stdout | trim }}"
