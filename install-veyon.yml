- hosts: paz
  tasks:
    - ansible.windows.win_shell: |
        curl.exe -L -o "$env:TEMP\veyon-setup.exe" "https://github.com/veyon/veyon/releases/download/v4.10.1/veyon-4.10.1.0-win64-setup.exe"
        Start-Process -FilePath "$env:TEMP\veyon-setup.exe" -ArgumentList "/S /NoMaster" -Wait
